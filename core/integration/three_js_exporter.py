class ThreeJsExporter:
    def export_mesh(self, object_id, format="GLB"):
        """Converts internal neural meshes to Three.js compatible formats."""
        print(f"[EXPORT] Converting unit {object_id} to {format}...")
        return b"MOCK_GLB_DATA"
